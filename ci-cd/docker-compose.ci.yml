name: expense-ci

services:
  app:
    build: ../backend
    environment:
      - PORT=3000
      - MONGO_URI=mongodb://mongo:27017/expense_tracker
    depends_on:
      - mongo

  mongo:
    image: mongo:7